# 评论备份站

评论站的**只读**静态备份，托管在 GitHub Pages。

- 访问：`index.html?u=<账号>`（如 `?u=ycy` / `?u=hxy` / `?u=share` / `?u=yanghao`）
- 数据：`data.json`，由 NAS 评论池导出，**不含任何凭据**
- 本页不写入任何远端数据；"已取过"只记录在本机浏览器 localStorage

主站（NAS）不受影响，此仓库仅为外网可用性兜底的备用入口。
